# Event inbox

Drop links (or pasted announcement text) for one-off meetings here, one
per line — anything goes: a project open house, a hearing notice, a
comment-window announcement.

Next Claude session: for each item below, extract the details into
`events/curated.yaml` (see the field docs there), delete the processed
line from this file, and leave the diff for Ian to review before pushing.

---

(nothing pending)
